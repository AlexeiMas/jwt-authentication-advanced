export enum ERoute {
  LoginPage = "/login",
  RegisterPage = "/register",
  PostsPage = "/posts",
}

export enum EApiRoute {
  login = "/auth/sign-in",
  register = "/auth/sign-up",
  logout = "/auth/logout",
  refresh = "/auth/refresh",
}
